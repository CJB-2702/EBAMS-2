2026-04-18 — Added `.cursor/skills/persistent-memory` skill for one-line append-only notes in this file.
2026-04-18 — Bootstrapped `app.core` (division/org/ownership + auditable user-scope links), `app.administration` portal with contexts/policy/seed_dev, login and static settings.
2026-04-18 — Added `app.public_app` with `/` homepage (login + session note), `/about/` and `/site/about/` filler about page; auth URLs default to `/`.
2026-04-18 — Split user assignment models into `app/core/models/user_assignments/`; on login snapshot ownership group ids and Django permission strings into the session via `user_logged_in`.
2026-04-18 — Data ownership UI lists divisions/orgs/ownership groups scoped to the actor’s memberships unless `is_admin_actor`.
2026-04-18 — Added gitignored `docs/users_and_passwords.md` and root `.gitignore` entry for seeded user reference.
2026-04-18 — Documented Django superuser (`admin` / `DJANGO_SUPERUSER_PASSWORD`) in `docs/users_and_passwords.md` for `/admin/`.
2026-04-18 — Installed django-jazzmin; added `jazzmin` before `django.contrib.admin` and `JAZZMIN_SETTINGS` in `app/config/settings.py`.
2026-04-19 — Added `core.models.groupings` (`DivisionOrganisation`, moved `OrganizationOwnershipGroup`), M2M division link via through table; migration `0002_division_organisation_and_groupings`; slug uniqueness is now global per organization.
2026-08-20 — Updated `app/public_app/templates/shared/topnav.html` to include all side navigation links across every application portal dropdown (Events, Dispatching, Maintenance, Assets, Parts, Procurement, Inventory, Administration, Extensions).
2026-04-18 — Merged `app.core` into `app.administration` (models, migrations, admin, auth_session); removed `app.core`; kept legacy `core_*` table names; migration `0003_contenttypes_from_core_app_label`.
2026-04-18 — `.cursor/rules/migration-strategy.mdc`: on any schema-affecting model change, delete project numbered `migrations/*.py` (keep `__init__.py`), wipe DB, `makemigrations` + `migrate` + `seed_dev`.
2026-04-18 — Added root `delete_database_rebuild_models.py` (`--seed`) and `.cursor/skills/delete-database-rebuild-models/SKILL.md`; migration rule points to the script.
2026-04-18 — `delete_database_rebuild_models.py`: explicit per-app `app/<name>/migrations` sweep; delete all files except `__init__.py`, drop `__pycache__`.
2026-04-18 — `migration-strategy.mdc`: no legacy-row cleanup in seeds; `seed_dev` dropped `_remove_legacy_demo_rows`.
2026-04-18 — `seed_dev`: 2 divisions, 4 orgs, 8 ownership groups; `super_admin` + legacy demo-* cleanup; scoped `generic_manager` to North; `users_and_passwords.md` aligned.
2026-04-18 — Reworked the administration portal UX toward Django-admin patterns: dashboard app list, sidebar shell, user changelist with filters/pagination/actions, and HTMX-powered ownership/permissions workflows with selected-user carryover.
2026-04-19 — Added user portal detail page at `user-portal/<id>/` (card layout: general info, action permissions with collapsible groups/direct perms, data permissions); user list rows open detail on click (checkboxes unchanged).
2026-04-19 — User portal detail switched from three-column cards to Bulma boxed tabs (General / Action permissions / Data permissions) with tab panels and keyboard nav.
2026-04-19 — User portal detail: `h3.title.is-6` field headers and spacing blocks to separate labels from values (general, action, data tabs).
2026-04-19 — User portal detail: single scrolling page with three full-width stacked cards (tabs removed); kept field headers inside each card.
2026-04-19 — Enabled `LoginRequiredMiddleware`, removed `@login_required` from administration entrypoints, marked `public_app` views with `@login_not_required` and `PublicLogoutView`.
2026-04-19 — Added `.cursor/rules/public-app-routes.mdc` (public pages only in `app/public_app`; otherwise login middleware).
2026-04-19 — Upgraded dependencies to Django 6.0.x (`requirements.txt` `Django>=6.0,<7`); `manage.py check` clean.
2026-04-19 — Added `generic_admin`-only user portal edit at `user-portal/<id>/edit/` (password, dual-listbox groups/permissions/ownership, reusing DjangoPermissionsContext and OwnershipContext); detail page shows Edit link for admins.
2026-04-19 — `is_admin_actor` / `is_grant_actor` treat Django superusers like full admins for portal policy; user portal edit and grant flows allow superusers.
2026-04-19 — User portal detail: list all effective permissions; each Django group is `<details>` with group name as summary and inherited group permission keys listed inside.
2026-04-19 — User portal edit: grey available permissions already granted via groups; `check_direct_permissions_against_group` POST endpoint + JS debounced check on select with warning panel.
2026-04-19 — Replaced fetch/JSON with HTMX + `_direct_perm_group_check.html` fragment; warning renders under Available column; `htmx:configRequest` CSRF from base template.
2026-04-19 — User portal edit Data permissions: divisions and organizations use dropdown + table (remove per row) instead of dual listbox; ownership groups unchanged.
2026-04-19 — UX_UI.md: added Modals section—prefer `<dialog>` with Bulma `.modal-card` inside and project reset class.
2026-04-19 — User portal edit organizations: add/remove org-only vs org+linked ownership groups (2:1 button widths); `OwnershipContext` overlap-safe removal when orgs share groups.
2026-04-19 — Added `app/static/app.css` (square Bulma radii, ~25% larger button type/padding, self-hosted IBM Plex Mono for form fields) and linked it from administration and public base templates plus login.
2026-04-19 — User portal index: removed bulk checkboxes and action dropdown/POST; list is filter + click row to open user only.
2026-04-19 — Data ownership index is browse-only with sectioned links; added division/org/ownership-group portals. Permissions portal replaced by access-management + permission-group portals; `permissions/` redirects to `access-management/`.
2026-04-19 — Vendored Material Icons TTF under `app/static/fonts/material-icons/`, `@font-face` + `.material-icons` in `app.css`; administration/public templates use icon-only on small actions (remove, disable, dual-listbox) and icon+label on default/full-width buttons.
2026-04-19 — Fixed static paths after grouping assets: login template uses `css/bulma.min.css`; `app.css` `@font-face` URLs use `../fonts/...` so fonts resolve from `static/fonts/` when the stylesheet is under `static/css/`.
2026-04-19 — Access management permissions: paginated (32/page) with HTMX + `hx-select` on `#access-permissions-results`; Bulma `pagination` (centered, rounded, elided page numbers via `Paginator.get_elided_page_range`).
2026-04-19 — Added `search-dropdown` web component, `administration_permissions` fragment GET (`format=htmx-search-results`, `group_id`, `q`, `page`), permission-group add-permission form wired to it; documented in `COMMON_UI_COMPONENTS.md`.
2026-04-19 — search-dropdown: default `hx-target` uses HTMX `global #…` so fragment swaps resolve from the document when the trigger is inside shadow DOM.
2026-04-19 — Permission group portal: dual-listbox users/permissions + `list_kind="user"`; standard `card` layout (no `<details>`) to fix overlap; `_dual_listbox`: `is-multiline`, `is-align-items-flex-start`, `icon-text` labels (Assign, Remove, Assign all, Remove all) on user edit and permission group.
2026-04-19 — Moved `delete_database_rebuild_models.py`, `generate_env.py`, and `memory.md` into `dev_tools/`; scripts resolve repo root via parent of `dev_tools/`; updated `.cursor` skills/rules, `settings.py`, and `docs/users_and_passwords.md`.
2026-04-19 — Moved `_dual_listbox.html` to `administration/templates/shared_components/`; includes updated in user edit and permission group portal.
2026-04-19 — User portal org scope block: labeled the two remove buttons (org+sweep groups vs org only) beside Material icons.
2026-04-22 — Access management index: `group_q` filter, fixed-height scroll areas, chunked infinite scroll via `format=htmx-access-groups-panel|append` and `htmx-access-permissions-panel|append` (replaced paginated permissions list).
2026-04-22 — Data ownership index: same card pattern (filter, scroll, `revealed` sentinel chunks) for divisions, organizations, and ownership groups via `format=htmx-data-*-panel|append` on `data_ownership_index`.
2026-04-22 — Data ownership division/org tables: counts only (annotated), `Manage division` / `Manage organization` buttons aligned with access-management portal actions.
2026-04-23 — Data ownership index queries moved to `presentation_layer/search/data_ownership_index_search.py` (scoped direct querysets, org division name via Subquery, no `reference_scope` list helpers or prefetch for that page).
2026-04-23 — Added `shared_components/_htmx_browse_filter_form.html` (HTMX debounced search + Filter + panel swap) and wired access-management and data-ownership index cards to use it.
2026-04-23 — Data ownership / access management: single-line `{% include %}` for `_htmx_browse_filter_form.html` (removed multiline `only`) so Django parses the tag instead of echoing it as HTML.
2026-04-23 — Ownership group portal: HTMX browse filters + chunked org/user lists (`format=htmx-og-organizations-panel|append`, `htmx-og-users-panel|append`); `_htmx_browse_filter_form` supports optional `hx_get_url`.
2026-04-22 — Division portal: dual-listbox for organizations (link/unlink via `division_structure` + policy) and for division users (`add_users` / `remove_users`); read-only card fallbacks when not grant actor.
2026-04-22 — Organization portal: dual-listbox for ownership groups (link/unlink via `OrganizationOwnershipGroup`) and for users (`add_users` / `remove_users` via `OwnershipContext`); same template pattern and script as division portal.
2026-04-22 — `csrf-htmx-required-snippet.html`: wrapped CSRF hook in `<script>`, fixed listener to `htmx:config:request` and `event.detail.ctx.request.headers` (HTMX 2).
2026-06-17 — Assets app wiring (mock_data → control layer), session 1 of 2. Established the per-domain pattern: guard(Validator) + adapter(Create/Edit) + domain_struct + factory + context(verbs) + presentation_layer/search + thin entrypoint. First-ever `app/assets/presentation_layer/search/` dir created here.
2026-06-17 — Manufacturers vertical wired end-to-end (new control stack; was missing entirely). Classes vertical wired (new control: domain-set sync + class-capability set reconcile on AssetClassContext). Seeded manufacturers + 3 CapabilityDefinitions in dev_assets_base.json. Both verified via test client.
2026-06-17 — Models vertical wired: extended AssetModelFactory (meter units) + AssetModelContext (update/manufacturer-sync/set_capabilities); new asset_model search + adaptor. Seeded 3 AssetModels + ModelManufacturer links. htmx-search-results branch preserved. Template fixes: display_name→{{obj}}, base→base_model, *.all on m2m, configuration_templates/modification_links.
2026-06-17 — Assets vertical wired (7 pages): create via AssetCreationOrchestrator; added AssetContext.update + new AssetImageManager (upload→events.File, set_primary, delete) exposed as context.images; new asset_search with load_asset_detail bundle (meters/images/capabilities/configuration as separate context vars — reverse relations can't be shadowed). Fixed AssetCreateAdaptor to read form names (domain/model/parent_id). Assets not fixture-seeded (FileSet/ActivityThread proxies); verified by creating through endpoint. All submits green incl. reparent+cycle-guard and image upload.
2026-06-17 — STILL MOCK (next session): Capabilities (4 submits: definition CRUD, assignment hub assign/remove, asset-capabilities edit, bulk management) and Configurations (5 submits: defined modification, template builder/edit, applicability, asset configuration) + dashboard read. Substantial existing control (capability_*/configuration_*/modification_*/applicability_* managers + guards) — audit before writing; wire/extend existing managers over new files.
2026-08-10 — Price observation mini kit, Phase 1 (schema): new `procurement.models.pricing` package (PartPriceObservation, PriceSourceType/PriceConfidence/UnitCostSource enums), 3 provenance columns on PurchaseOrderLine, PERM_PRICE_ESTABLISH + `price_establish` custom permission, dev group grants. Applied D87–D92 kit-drift corrections from price_observation_mini_kit/build_plan.md §1 (domain non-nullable, two-fact resolution not a 3-rung ladder, no pricing_basis column).
2026-08-10 — Price observation mini kit, Phase 2 (control layer): PartPriceObservationValidator guard, Factory/BulkFactory (all-or-nothing bulk_create), PartPriceGridAdaptor (money parsing, dropped-row tracking, session handoff), PartPriceNarrator (staleness phrasing). 14 new tests in test_price_observations.py, full procurement suite green (23 tests). Full reset + `manage.py check` clean.
2026-08-10 — Price observation mini kit, Phase 3 (session tools): recent_part_creations.py (D81 anti-pattern, 3-function surface, append/dedupe/TTL/100-cap) and price_grid_draft.py wired into parts.py + parts_bulk_upload.py via transaction.on_commit.
2026-08-10 — Price observation mini kit, Phase 4 (reads/routes/pages): part_visibility.py (§2.4 predicate), unpriced_part_search.py, part_price_history_search.py, prices.py entrypoints (hub/bulk grid/unpriced/history), urls_prices.py, 11 templates, hub entry card + unpriced stat tile on shell.py.
2026-08-10 — Price observation mini kit, Phase 5 (chip + picker): PartPriceFactsStruct (two facts, no ranking per D91), PartPricePolicy.facts_for/facts_for_many, wired into purchase_orders.py create+detail (O(1)-query bulk resolve), htmx-picker fragment (D92, capped 50, unfiltered).
2026-08-11 — Price observation mini kit, Phase 7 (ordered writer): PurchaseOrderContext.place() now writes one `ordered` PartPriceObservation per line via the existing bulk factory, inside the same atomic block as the status move — closes the last open box in build_plan.md §9's first-cut Definition of Done. Blank line confidence falls back to UNKNOWN (guard requires a real band, line default is blank/"not stated"). 2 new tests.
2026-08-11 — Price observation mini kit, loose ends: request-boundary breadcrumb tests (session save/load through SessionMiddleware across two request objects, plus an on_commit-rollback test) in test_recent_part_creations.py — the test build_plan.md §5 called "the test that matters" since an in-memory dict assertion passes against the nested-mutation hazard. Logged D89 (price_establish is a global perm, not per-domain) and D90 (soft lock on line override) as tech debt in the new `docs/procurement/tech_debt/`, with # TODO(D90) added at its call site in purchase_orders.py (D89's TODO was already in the guard from Phase 1). Also logged D79 (parties/ extraction) per README's explicit ask. Full procurement suite: 29 tests green, `manage.py check` clean. Phases 6/8/9 remain out of scope (not part of the kit's first-cut DoD).
2026-08-16 — Inventory Phase 3, three-tier SVG spatial engine (FD-29, supersedes FD-22/FD-24's "why"): new `RoomLocation` model between Room and StorageLocation (XY tier); Room/RoomLocation each carry photo_gallery/current_layout (events.FileSet/Attachment, mirrors Asset's gallery pair) replacing the flat `Room.svg_layout` text column. New RoomSvgAdapter (sanitize/extract/normalize/inject, one implementation for both tiers via group_label param, exact-match only — no prefix fallback). TopographyContext gained upload_room_layout/upload_room_location_layout + reconciliation (SvgReconciliationStruct: matched/unmatched/orphaned), archives only sanitized SVG (raw upload never persisted). New warehouse/room/room_location entrypoints+templates (topography.py), inventory_svg.css. Relabeled 2 real legacy Inkscape fixtures (room_layout_los_angeles.svg, room_location_layout_bins.svg) under app/inventory/tests/fixtures/, wired into seed_inventory_dev.py's new "Spatial Map Demo" room via the real control-layer path. 49 inventory tests green; full manual click-through verified (shape click, F5 state, skip-tier-3 shortcut, Z-picker drill-down, upload+reconcile+create-selected).
2026-08-16 — Inventory Phase 4, Intake Engine Core & Auto Intake Portal (FD-6/9/10/11/12/13/27/28), resumed after a crash mid-build: new `IntakeContext` (start/associate/create_manual_allocation/split_allocation/transition_to_reconciliation/resolve_line/close_session/cancel_session + `commit_auto_intake`), `IntakeCommitOrchestrator` (the ONLY caller of `ShipmentContext.accept_line`/`split_line` and `StockLedgerManager.inject` on session close — cumulative-accept math across every CLOSED session, reconciliation barrier, unmanifested quarantine), `AutoIntakeManager` (monotonic floor/cap/delta math per the workflow guide's worked examples A-C), `ReconciliationManager` (parent+child grain, FD-9), `intake_guard.py` (StateMachine/AllocationValidator/AutoIntakeValidator/IntakePolicy), first `narrators/` dir in this app, 5 domain structs. Found and fixed two stale-work gaps from the crashed run: inventory's migration never actually contained the 5 intake models and procurement's never had `ShipmentLine.comments` despite the resume brief claiming otherwise (full `refresh_project.py` reset fixed both), and `ShipmentContext` was missing the `split_line` verb wrapper plus `ShipmentNarrator.line_split` entirely. Routes: `/inventory/intake/`, `/inventory/intake/auto/` (format=htmx-search-results, format=htmx-shipment-lines, POST→303), `/inventory/intake/session/<id>/` — all placeholder Bulma templates flagged `TODO frontend-engineer` per auto_intake_workflow_guide.md's mockup. Seeded 3 scenarios (full receipt, partial receipt+split, unmanifested overage) through the real control-layer path; `can_intake_stock` granted to the dev `generic_admin` group. 160 inventory+procurement tests green (15 new, in test_auto_intake_math.py and test_intake.py), full Auto Intake round-trip verified live via the real POST endpoint (partial receive → floors reflect existing → second receive tops up cumulatively to the full shipped qty, 20/20).
2026-08-16 — Inventory Phase 4 frontend pass: restyled all 5 intake templates (dashboard, auto.html, session_detail, _shipment_search_results, _shipment_lines) to established Bulma/HTMX conventions (page-header-icon hero, body-grid, custom-card-footer, htmx-indicator-overlay). Fixed the flagged Room-select gap — was a raw numeric input, now a proper `<select>` scoped to the chosen Warehouse via a new format=htmx-room-select fragment (warehouse's onchange hx-get re-renders it; no hx-push-url on that exchange since it's fragment-only and would break F5). Added a server-computed live delta/cap preview: htmx-shipment-lines now reads accepted_<id>/rejected_<id> from the request and re-renders via AutoIntakeManager.compute_delta per row (hx-trigger=change on every target input, hx-include gathers the whole matrix) — no client-side validation math, server stays sole authority. Added existing-allocation subtree per line (ItemAllocation read, session-tagged). Reconciliation card follows rule 5 (always renders, explicit Phase-5-not-yet-available text). Verified live via dev-login: dashboard → auto intake → search → select shipment → lines matrix incl. cap-violation preview → submit → 303 → session detail, F5 at each step, all 3 seeded scenarios' session-detail pages, and a real POST commit (5+3=8.000 accepted, 2 allocation rows). 90 app.inventory tests green throughout.
2026-08-16 — Updated Active Inventory storage location filter to use `<search-dropdown>` web component backed by `storage_location_search` endpoint.
2026-08-17 — Inventory Phase 6 terminal issue sessions: added PartIssueSession entity (ISS-YYYYMMDD-HEX header, IssueSessionStatus.COMMITTED), updated PartIssue FK, PartIssuanceOrchestrator.commit_session, updated issuance_portal to Issue Parts Workspace with F5 draft queue, and updated issues_index to Issued Parts Ledger (Committed Sessions tab + Granular Line Items tab + session_detail.html receipt page).
2026-08-18 — Ported Maintenance Plans UI (Index, Detail, Create, Edit, Worklist) with Many-to-Many models schema, custom filters, and HTMX-backed dual listbox; changed template index to default to preview split with auto-selection of first template.
2026-08-20 — Updated `app/public_app/templates/shared/topnav.html` to include all side navigation links across every application portal dropdown (Events, Dispatching, Maintenance, Assets, Parts, Procurement, Inventory, Administration, Extensions).
2026-08-25 — Ported the legacy Flask storeroom designer into inventory topography. Mapped legacy Storeroom→Room, Location→RoomLocation, Bin→StorageLocation, with Warehouse as a new tier above. Built warehouse CRUD (`/inventory/warehouses/`, `/warehouses/create/`, `/warehouse/<pk>/edit/`) with retire/reactivate instead of any hard delete, plus room add/edit/retire and manual add/retire of locations and bins on the two `/layout/` builder routes. Added three stock-protection Policies in `topography_guard.py`: `StorageLocationPolicy` (no retiring a bin holding stock — legacy had no bin check at all), `RoomLocationPolicy` (same rule one tier up so the parent isn't a way around it), and `LayoutReconciliationPolicy` (an upload whose shapes drop a stock-holding location is rejected before `_archive_layout`, so `current_layout` is untouched; `allow_orphaned_stock=True` is the override seam, no UI path). Every write now behind `inventory.can_manage_topography`, added to the `generic_admin` + `inventory_admin` dev fixture groups. Found and fixed a real rendering bug while porting: Inkscape declares the SVG namespace twice (`xmlns` and `xmlns:svg`), lxml re-serialized everything as `<svg:svg>`/`<svg:rect>`, and browsers don't resolve that to SVG in an HTML document — every Inkscape-authored layout in the app rendered as a flat strip of label text. `RoomSvgAdapter._strip_svg_prefix` applied on both sanitize and normalize_viewbox so pre-existing attachments repair on read. 127 inventory tests green (19 new in test_topography_stock_guards.py, 3 in test_room_svg_adapter.py); reject path verified live via a real upload POST. Port record in docs/inventory/storeroom_designer_port/. Known gap: legacy SVGs label shapes unpadded (`1-1`) while display_code is padded (`0001-0001`), so a legacy file reconciles as all-unmatched — flagged, not fixed.
